CREATE TABLE EVENTOS(
	ID nvarchar(40) NOT NULL,
	CURSOID nvarchar(40) NULL, 
	USUARIOID nvarchar(40) NULL, 
	TITULO nvarchar(255) NOT NULL,
	TIPOEVENTOID nvarchar(40) NOT NULL,
	DESCRICAO nvarchar(255) NOT NULL,
	PONTOS INT NULL,
	PALESTRANTE nvarchar(255) NOT NULL,
	LOCALEVENTO nvarchar(255) NOT NULL,
	DATAHORARIOINICIO datetime NOT NULL,
	DURACAO decimal (9,2) NOT NULL,
	ATIVO bit NULL DEFAULT((1))
)
GO

CREATE TABLE TIPOEVENTO(
	ID NVARCHAR(40) NOT NULL,
	CONSTRAINT Pk_ID_TipoEvento PRIMARY KEY (ID),
	NOMETIPO NVARCHAR(100) NOT NULL,
	DESCRICAO NVARCHAR(255) NOT NULL
)
GO

ALTER TABLE EVENTOS
ADD CONSTRAINT Pk_Id_Eventos PRIMARY KEY (ID); /* Configurando chave-primária */

ALTER TABLE EVENTOS
ADD CONSTRAINT Fk_CursoId_Eventos FOREIGN KEY (CURSOID) REFERENCES CURSO (ID); /* Relacionar o evento a um curso específico */

ALTER TABLE EVENTOS
ADD CONSTRAINT Fk_UsuarioId_Eventos FOREIGN KEY (USUARIOID) REFERENCES USUARIO (ID); /* Atrelar um organizador ao evento */

ALTER TABLE EVENTOS
ADD CONSTRAINT Fk_TipoEventoId_Eventos FOREIGN KEY (TIPOEVENTOID) REFERENCES TIPOEVENTO (ID); /* Classificar o tipo evento */