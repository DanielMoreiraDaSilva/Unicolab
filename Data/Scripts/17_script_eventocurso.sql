CREATE TABLE EVENTOCURSO(
	ID nvarchar(40) NOT NULL PRIMARY KEY,
	CURSOID nvarchar(40) NOT NULL FOREIGN KEY REFERENCES CURSO (ID), /* CHAVE ESTRANGEIRA DA TABELA CURSO */
	EVENTOID nvarchar(40) NOT NULL FOREIGN KEY REFERENCES EVENTOS (ID) /* CHAVE ESTRANGEIRA DA TABELA EVENTOS */
)
GO