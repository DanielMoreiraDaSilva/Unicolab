CREATE TABLE OPORTUNIDADECURSO(
	ID NVARCHAR(40) NOT NULL,
	CONSTRAINT Pk_ID_OportunidadeCurso PRIMARY KEY (ID),
	CURSOID NVARCHAR(40) NOT NULL,
	OPORTUNIDADEID NVARCHAR (40) NOT NULL
)

GO

ALTER TABLE	OPORTUNIDADECURSO
ADD CONSTRAINT FK_CURSOID_OPORTUNIDADECURSO FOREIGN KEY (CURSOID) REFERENCES CURSO (ID);

ALTER TABLE	OPORTUNIDADECURSO
ADD CONSTRAINT FK_OPORTUNIDADEID_OPORTUNIDADECURSO FOREIGN KEY (OPORTUNIDADEID) REFERENCES OPORTUNIDADE (ID);