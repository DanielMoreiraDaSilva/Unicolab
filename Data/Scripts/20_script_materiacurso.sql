CREATE TABLE MATERIACURSO (
	ID NVARCHAR(40) NOT NULL PRIMARY KEY,
	MATERIAID NVARCHAR(40) NOT NULL FOREIGN KEY REFERENCES MATERIA (ID),
	CURSOID NVARCHAR(40) NOT NULL FOREIGN KEY REFERENCES CURSO (ID)
	)
GO

ALTER TABLE EVENTO ALTER COLUMN DURACAO INT NOT NULL;